{
  "name": "trackflow",
  "version": "1.0.0",
  "private": true,
  "description": "TrackFlow — Web Project Management & Desktop Time Tracker",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "db:migrate": "turbo run db:migrate",
    "clean": "turbo run clean && rimraf node_modules"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "turbo": "^2.0.14"
  },
  "packageManager": "pnpm@10.33.0"
}
